---
name: 060_10 Instrument List Command
overview: Добавить в CLI команду просмотра списка инструментов для выбранного MIDI-движка, при этом для `mido + python-rtmidi` использовать справочный GM-каталог и явно документировать ограничение внешних синтов.
todos:
  - id: add-cli-flag-list-instruments
    content: Добавить CLI-флаг(и) для вывода списка инструментов и раннего выхода из программы.
    status: completed
  - id: implement-gm-reference-source
    content: Реализовать источник GM-списка (program/name) для ветки mido + python-rtmidi.
    status: completed
  - id: wire-output-and-engine-note
    content: Собрать формат вывода списка и добавить пояснение об ограничениях внешнего MIDI-синта.
    status: completed
  - id: update-readme-examples
    content: Обновить README примерами команды и ожидаемым поведением.
    status: completed
isProject: false
---

# План 060_10: команда списка инструментов (mido + python-rtmidi)

## Цель
Сделать быстрый и предсказуемый способ получить список инструментов из CLI, не заходя в listening mode: для `mido + python-rtmidi` показывать General MIDI reference list и пояснение, что фактические патчи задаются внешним синтом/DAW.

## Область изменений
- Добавить CLI-флаг в [d:\R_STUDIO\PRG\python\2026-05-23-midi-synt\main.py](d:\R_STUDIO\PRG\python\2026-05-23-midi-synt\main.py) для вывода списка инструментов.
- Вынести/добавить источник данных GM-инструментов (минимум: `program`, `name`, опционально `family`) рядом с логикой CLI в [d:\R_STUDIO\PRG\python\2026-05-23-midi-synt\main.py](d:\R_STUDIO\PRG\python\2026-05-23-midi-synt\main.py) или в отдельный модуль проекта.
- Обновить пользовательские примеры запуска в [d:\R_STUDIO\PRG\python\2026-05-23-midi-synt\README.md](d:\R_STUDIO\PRG\python\2026-05-23-midi-synt\README.md).

## Шаги реализации
1. Расширить аргументы CLI:
   - `--list-instruments` для текстового вывода в stdout.
   - (опционально, если уже есть pattern в проекте) `--list-instruments-json` для машиночитаемого вывода.
2. Реализовать генерацию списка для ветки `mido + python-rtmidi`:
   - использовать фиксированный GM-набор (128 программ, bank 0);
   - добавить заметку в вывод: "для external MIDI output реальный набор зависит от синта/плагина".
3. Встроить ранний выход из `main`:
   - при вызове флага список печатается и приложение завершается без старта live-потока.
4. Обновить README:
   - команда запуска;
   - пример фрагмента вывода;
   - ограничение по `midi-out` (нет универсального API перечисления пресетов внешнего устройства).

## Критерии готовности
- `python main.py --engine midi-out --list-instruments` печатает непустой GM-список.
- Вывод содержит понятную пометку про ограничение внешнего синта.
- Команда завершает процесс без запуска listening/live цикла.
- README содержит рабочий пример этой команды.

## Риски
- Несовпадение GM-номеров с конкретным внешним устройством: это ожидаемо; закрывается явной пометкой в CLI и документации.
- Разный формат вывода (текст/JSON) может повлиять на будущую автоматизацию: минимально зафиксировать стабильные поля (`program`, `name`) при JSON-режиме.