---
name: Горячие клавиши избранных
overview: Добавить выбор избранных программ по F-клавишам (1–20 позиция в списке избранного) и вывод сообщения, если нужной позиции нет. Обновить обработку клавиш, подсказки и тесты для нового поведения.
todos:
  - id: extend-keyboard-actions
    content: Добавить в poll_keyboard_actions распознавание F1..F10 и Shift+F1..Shift+F10 с выдачей индекса избранного 1..20
    status: completed
  - id: handle-favorite-select
    content: Расширить KeyboardAction и обработчик в run() для выбора избранного по индексу и сообщения при отсутствии позиции
    status: completed
  - id: update-hotkeys-help
    content: Обновить print_hotkeys и стартовую подсказку в run() для новых комбинаций
    status: completed
  - id: add-tests
    content: Добавить/обновить тесты для подсказок и маппинга F-клавиш, затем прогнать run_tests.bat
    status: completed
isProject: false
---

# План: F1/F10 для избранных программ

## Что меняем
- Расширить обработку клавиатуры в [d:\R_STUDIO\PRG\python\2026-05-23-midi-synt\main.py](d:\R_STUDIO\PRG\python\2026-05-23-midi-synt\main.py):
  - Добавить распознавание `F1..F10` и `Shift+F1..Shift+F10` в `poll_keyboard_actions()`.
  - Преобразовывать эти нажатия в действие выбора избранного по индексу: `1..20` (позиция в списке, где `F1` = 1-й избранный, `Shift+F1` = 11-й).
- Расширить `KeyboardAction` новым типом действия для выбора избранного по индексу (например, `favorite_select_index`) и полем индекса.
- В основном цикле `run()` добавить обработчик нового действия:
  - Если индекс существует в `favorites`, выбрать соответствующую программу через `program_controller.set_program(...)` и вывести текущий формат подтверждения выбора.
  - Если индекса нет, вывести сообщение в том же стиле, что и текущие подсказки для избранного (по аналогии с `"Нет избранных, добавьте в избранные через *"`).

## Сообщение при отсутствии позиции
- Использовать единый текст в стиле текущего CLI, например:
  - `Нет избранной программы №N, добавьте в избранные через *`
- Это покрывает и пустой список, и случай, когда список короче N.

## Обновление подсказок
- Обновить `print_hotkeys()` в [d:\R_STUDIO\PRG\python\2026-05-23-midi-synt\main.py](d:\R_STUDIO\PRG\python\2026-05-23-midi-synt\main.py), добавив строки про:
  - `F1..F10` → 1–10 избранные.
  - `Shift+F1..Shift+F10` → 11–20 избранные.
- Обновить стартовую строку с быстрыми клавишами в `run()` (блок приветствия), чтобы новые шорткаты были видны сразу после запуска.

## Тесты
- Обновить/добавить тесты в [d:\R_STUDIO\PRG\python\2026-05-23-midi-synt\tests\test_program_names.py](d:\R_STUDIO\PRG\python\2026-05-23-midi-synt\tests\test_program_names.py) для новых строк в `print_hotkeys()`.
- Добавить модульные тесты обработки клавиш (в существующий тестовый файл или новый):
  - `F1` и `F10` мапятся в индексы 1 и 10.
  - `Shift+F1` и `Shift+F10` мапятся в индексы 11 и 20.
  - При отсутствии нужной позиции формируется корректное сообщение.

## Проверка после реализации
- Прогнать локальные тесты (`run_tests.bat`) и убедиться, что:
  - Старые горячие клавиши работают как раньше.
  - Новые F-клавиши корректно выбирают элементы избранного по номеру позиции.
  - Сообщение об отсутствующей позиции выводится стабильно и понятно.